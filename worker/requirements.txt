# Chạy trên Colab GPU (2026-09: Python 3.13, CUDA 12.8). KHÔNG cài trên máy dev —
# test phải mock các lib nặng.
#
# KHÔNG pin torch/torchaudio. Lý do (đo trên Colab T4 ngày 2026-09-05):
#   - Colab đã cài sẵn torch/torchaudio khớp CUDA của runtime.
#   - whisperx tự kéo bản torch nó cần (3.8.6 -> torch 2.8.0+cu128, cuda OK).
#   - Bản cũ pin torch==2.1.2 làm `pip install -r` FAIL SẠCH trên Python 3.13
#     (PyPI chỉ còn từ 2.5.0) -> không lib nào được cài.

# Audio Separation
demucs==4.1.0

# Speech Recognition (KHÔNG dùng openai-whisper)
whisperx==3.8.6
faster-whisper==1.2.1
ctranslate2==4.8.2

# Video — để dải version, Colab cài sẵn numpy 2.2.6 / Pillow 11.3.0.
# Pin cứng bản cũ (numpy 1.26.4, Pillow 10.4.0) không có wheel cho Python 3.13.
Pillow>=11.0,<12
numpy>=2.0,<3

# Audio utils
soundfile>=0.12
librosa>=0.10
pydub>=0.25

# System
ffmpeg-python>=0.2
# FFmpeg binary cài bằng apt trong notebook, không phải pip
